# Retrieval

The platform supports heading-aware chunking and fixed-size chunking. Heading-aware chunking keeps document sections together so retrieved context preserves operational meaning.

Retrieval quality is measured with recall at k over a small evaluation set. Production deployments should expand the evaluation set with real support questions and expected source documents.

